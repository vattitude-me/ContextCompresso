# ContextCompresso
This is a lightweight Java proxy that compresses LLM context - tool outputs, logs, and conversation history before sending to any AI API, cutting token usage without changing your existing code.
